# SFMC Toolkit Labs - Environment Configuration Example
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. Create a Connected App in Marketing Cloud Setup > Apps > Installed Packages
# 3. Replace the placeholder values below with your actual credentials
# 4. Never commit .env.local to version control!

# Marketing Cloud Connected App Credentials
CLIENT_ID=your_client_id_here
CLIENT_SECRET=your_client_secret_here
AUTH_DOMAIN=your_mc_subdomain.auth.marketingcloudapis.com
REDIRECT_URI=http://localhost:3001/auth/callback
BASE_URL=http://localhost:3001
PORT=3001

# Connected App Permissions Required:
# - Email: Read, Write, Send
# - Web: Read, Write  
# - Data Extensions: Read, Write
# - Tracking: Read
# - Journeys: Read, Write
# - List And Subscribers: Read, Write
# - File Locations: Read, Write
# - Saved Content: Read, Write

# For production deployment:
# - Use secure environment variable injection (Render, Heroku, etc.)
# - Never use hardcoded credentials
# - Use proper subdomain for AUTH_DOMAIN and BASE_URL/REDIRECT_URI
